# Headless UI Combobox with `immediate`

```
npm install
npm run dev
```

1. the combobox should open immediately because of `immediate` + `autoFocus`
2. press Escape to close the combobox
3. notice the lingering `data-open` attribute on the combobox input element, it goes away upon changing hover state
